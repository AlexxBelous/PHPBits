<?php
// question-3
?>
<div class="question-template"><p>Для каждого URL получить отдельные части:
        scheme (http / https)
        host
        port (если есть)
        path
        query (в виде массива, например, ['user' => 123, 'ref' => 'google'])
        Результат должен быть в виде массива массивов,
        где каждый элемент — это ассоциативный массив
        с разобранными частями URL.</p>
</div>
